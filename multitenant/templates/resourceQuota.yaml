{{- range .Values.tennants }}
{{- range $name, $values := . }}
#=================== Ressource quota ===================
apiVersion: v1
kind: ResourceQuota
metadata:
  namespace: {{ $name }}
  name: {{$name}}-quota
spec:
  hard:
{{- range $key,$val := $values.limits }}
    {{ $key }} : {{ $val }}{{ end }}
---
{{ end }}
{{ end }}
