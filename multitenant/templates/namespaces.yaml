#This namespace should already be present
#This is just to modify the label, to make it easier to whitelist in networkpolicies

apiVersion: v1
kind: Namespace
metadata:
  name: ingress-nginx
  labels:
    name: ingress-nginx

---

{{- range .Values.tennants }}

{{- range $name, $values := . }}

#=================== Namespace ===================
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $name }}
  labels:
    name: {{ $name }}

---

{{ end }}
{{ end }}
